# Only SSH public keys are accepted. Administration is performed through the
# deploy-admin account and sudo; direct root and password-based access are off.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
ChallengeResponseAuthentication no
X11Forwarding no
