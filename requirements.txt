openai>=1.0.0
python-dotenv>=1.0.0
# PostgreSQL driver for DATABASE_URL=postgresql://... in production. SQLite is built in for local development.
psycopg[binary]>=3.2.0
celery[redis]>=5.4.0
boto3>=1.35.0
pypdf>=4.0.0
PyMuPDF>=1.24.0
reportlab>=4.0.0
paper-search-mcp>=0.1.3
# Optional enhanced PDF parser for PDF_PARSER=auto/opendataloader. Requires Java 11+.
# opendataloader-pdf>=2.4.7
